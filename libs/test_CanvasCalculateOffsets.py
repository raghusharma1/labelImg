# ********RoostGPT********
"""
Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=calculate_offsets_9e01d9be0a
ROOST_METHOD_SIG_HASH=calculate_offsets_6acdb8ae67


Scenario 1: Verifying the calculation of offsets for a given shape and point
Details:
  TestName: test_calculate_offset_point_on_top_left_corner
  Description: Testing if the function correctly calculates offset when the given point lies on top-left corner of the shape's bounding rectangle. In this case, offset1 should be (0,0) and offset2 should be (width of rect, height of rect).
Execution:
  Arrange: Initialize a non-empty shape with a known bounding rectangle and a point positioned on the high-end corner of the rectangle.
  Act: Invoke the calculate_offsets function with the shape and point as parameters.
  Assert: Check the offsets should be (0, 0), (width, height).
Validation:
  The test allows us to verify the function's correctness when calculating offsets for a point on the top-left corner of a rectangle. As the point is on the top corner, the distance to the rectangle should be zero, validating the function under such scenario and condition.

Scenario 2: Verifying the calculation for a point inside the bounding rect of shape.
Details:
  TestName: test_calculate_offsets_point_inside_rect
  Description: This test case verifies the function for a point located inside the rectangle.
Execution:
  Arrange: Initialize a non-empty shape with a known bounding rectangle and a point placed inside the rectangle.
  Act: Call the calculate_offsets function with the shape and point as parameters.
  Assert: Verify if the offsets are correctly calculated considering the point's location within the bounding rectangle.
Validation:
  In this scenario, even if the point is inside the rectangle, the distance should be calculated correctly. The result will allow to validate the function behavior when given a point inside the rectangle.

Scenario 3: Verifying the calculation for a point outside the bounding rect of shape.
Details:
  TestName: test_calculate_offset_point_outside_rect
  Description: This test case verifies the function for a point located outside the rectangle.
Execution:
  Arrange: Initialize a non-empty shape with a known bounding rectangle and a point placed outside the rectangle.
  Act: Call the calculate_offsets function with the shape and point as parameters.
  Assert: Verify if the offsets are correctly calculated considering the point's location outside the bounding rectangle.
Validation:
  Inputs like these help validate the function behavior when given a point outside the rectangle. Even if the point is outside the rectangle, the distance to the rectangle should be negative.

Scenario 4: Verifying the calculation for a point on the bottom-right corner of the bounding rect of shape.
Details:
  TestName: test_calculate_offset_point_on_bottom_right_corner
  Description: This test case verifies the function for a point located on the bottom-right corner of the rectangle.
Execution:
  Arrange: Initialize a non-empty shape with a known bounding rectangle and a point on the bottom-right corner of the bounding rectangle.
  Act: Call the calculate_offsets function with the shape and point as parameters.
  Assert: Verify if the offsets are correctly calculated for the point location. 
Validation: 
  Offsets should be (-width, -height) and (0, 0). The test case allows us to verify how the function operates in such niche cases, and ensure it’s accurate in all scenarios.
"""

# ********RoostGPT********
from PyQt5.QtGui import *
from PyQt5.QtCore import *
from PyQt5.QtWidgets import *
from libs.shape import Shape
import pytest

# Define Canvas Class with the required method
class Canvas:
    def __init__(self):
        self.offsets = None

    def calculate_offsets(self, shape, point):
        rect = shape.bounding_rect()
        x1 = rect.x() - point.x()
        y1 = rect.y() - point.y()
        x2 = (rect.x() + rect.width()) - point.x()
        y2 = (rect.y() + rect.height()) - point.y()
        self.offsets = QPointF(x1, y1), QPointF(x2, y2)

class Test_CanvasCalculateOffsets:

    @pytest.mark.positive
    def test_calculate_offset_point_on_top_left_corner(self):
        #Arrange
        rect = QRectF(10, 10, 50, 50)
        shape = Shape()
        shape.add_point(rect.topLeft())
        shape.add_point(rect.bottomRight())
        point = QPointF(10, 10)
        canvas = Canvas()

        #Act
        canvas.calculate_offsets(shape, point)

        #Assert
        assert canvas.offsets == (QPointF(0, 0), QPointF(50, 50))

    @pytest.mark.positive
    def test_calculate_offsets_point_inside_rect(self):
        #Arrange
        rect = QRectF(10, 10, 50, 50)
        shape = Shape()
        shape.add_point(rect.topLeft())
        shape.add_point(rect.bottomRight())
        point = QPointF(20, 20)
        canvas = Canvas()

        #Act
        canvas.calculate_offsets(shape, point)

        #Assert
        assert canvas.offsets == (QPointF(-10, -10), QPointF(40, 40))

    @pytest.mark.negative
    def test_calculate_offset_point_outside_rect(self):
        #Arrange
        rect = QRectF(10, 10, 50, 50)
        shape = Shape()
        shape.add_point(rect.topLeft())
        shape.add_point(rect.bottomRight())
        point = QPointF(5, 5)
        canvas = Canvas()

        #Act
        canvas.calculate_offsets(shape, point)

        #Assert
        assert canvas.offsets == (QPointF(-5, -5), QPointF(55, 55))

    @pytest.mark.positive
    def test_calculate_offset_point_on_bottom_right_corner(self):
        #Arrange
        rect = QRectF(10, 10, 50, 50)
        shape = Shape()
        shape.add_point(rect.topLeft())
        shape.add_point(rect.bottomRight())
        point = QPointF(60, 60)
        canvas = Canvas()

        #Act
        canvas.calculate_offsets(shape, point)

        #Assert
        assert canvas.offsets == (QPointF(-50, -50), QPointF(0, 0))
