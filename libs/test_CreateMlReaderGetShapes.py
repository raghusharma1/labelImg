# ********RoostGPT********
"""
Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=get_shapes_b7f01a2e37
ROOST_METHOD_SIG_HASH=get_shapes_bc815932c4


Scenario 1: Verify the correct shapes returned in default scenario
Details:
  TestName: test_get_shapes_default
  Description: This test is intended to verify that the get_shapes method returns the correct shapes assigned to the respective instance.
Execution:
  Arrange: Create an instance of the class with a mock 'shapes' attribute.
  Act: Call the get_shapes method without providing any parameters (as none are required).
  Assert: Check that the returned value is equal to the initial mock 'shapes' value.
Validation:
  It's crucial because it ensures the correct shapes are returned, which is a central functionality of the get_shapes method according to the business logic.

Scenario 2: Check if get_shapes returns an empty list when no shapes are stored in the instance
Details:
  TestName: test_get_shapes_empty
  Description: This test is to verify if the get_shapes method returns an empty list if no shapes have been stored in the instance.
Execution:
  Arrange: Create an instance of the class, making sure the 'shapes' attribute is an empty list.
  Act: Call the get_shapes method on the instance.
  Assert: Ensure that the returned value is an empty list.
Validation:
  It's important to ensure the function behaves as expected under empty scenarios. It aligns with the business requirement that an empty list should be returned when no shapes have been added to the instance.

Scenario 3: Check the get_shapes return value when multiple shapes are stored in the instance
Details:
  TestName: test_get_shapes_multiple
  Description: This test is to verify if the get_shapes method returns a list of all shapes that have been stored in the instance.
Execution:
  Arrange: Create an instance of the class, with 'shapes' attribute having multiple mocked shape entries.
  Act: Call the get_shapes method on the instance.
  Assert: Check that the returned value is equal to the initial list of mock shapes we added to the 'shapes' attribute.
Validation:
  It's important to ensure the function behaves as expected under multiple inputs. It aligns with the business requirement that all shapes should be returned when shapes have been added to the instance.
"""

# ********RoostGPT********
import pytest
import json
from pathlib import Path
from libs.constants import DEFAULT_ENCODING
import os
from create_ml_io import CreateMLReader

class Test_CreateMlReaderGetShapes:

    def test_get_shapes_default(self):
        mock_shapes = ['circle', 'square', 'rectangle']
        create_ml_reader = CreateMLReader('folder', 'file', 'img_size', mock_shapes, 'output_file')
        assert create_ml_reader.get_shapes() == mock_shapes

    def test_get_shapes_empty(self):
        mock_shapes = []
        create_ml_reader = CreateMLReader('folder', 'file', 'img_size', mock_shapes, 'output_file')
        assert create_ml_reader.get_shapes() == mock_shapes

    def test_get_shapes_multiple(self):
        mock_shapes = ['circle', 'square', 'rectangle', 'triangle', 'hexagon']
        create_ml_reader = CreateMLReader('folder', 'file', 'img_size', mock_shapes, 'output_file')
        assert create_ml_reader.get_shapes() == mock_shapes
