# ********RoostGPT********
"""
Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=__init___7e8a4bb0b9
ROOST_METHOD_SIG_HASH=__init___57c9c01d5c


```
Scenario 1: Test the behaviour of __init__ function when a correct json_path and file_path is passed.
Details:
  TestName: test_correct_init
  Description: This test is intended to verify the successful initialisation of the function.
Execution:
  Arrange: Pass a valid json_path and file_path to the __init__ method.
  Act: Invoke the __init__ method with the valid parameters.
  Assert: Ensure no exceptions occur and the filename attribute is parsed from file_path.
Validation:
  This checks the functionality of json parsing and filename extraction, therefore ensuring the business requirement of reading from files is met.

Scenario 2: Test the behaviour of __init__ function when an incorrect (non-existing) json_path is passed.
Details:
  TestName: test_incorrect_json_path
  Description: This test is intended to verify the behaviour of `__init__` when the JSON file does not exist.
Execution:
  Arrange: Pass in a json_path that does not exist to the `__init__` method.
  Act: Invoke the `__init__` method with the invalid parameters.
  Assert: Ensure the function raises a FileNotFoundError.
Validation:
  This checks the business requirement and functionality of the program to handle invalid file paths correctly and inform the user accordingly.

Scenario 3: Test the behaviour of __init__ function when a json_path with invalid JSON content is passed.
Details:
  TestName: test_invalid_json
  Description: This test is intended to verify the behaviour of `__init__` when the JSON content is not valid.
Execution:
  Arrange: Create a JSON file with invalid or corrupt content and provide it as the json_path to the `__init__` method.
  Act: Invoke the `__init__` method with the invalid JSON.
  Assert: Ensure the function prints: "JSON decoding failed".
Validation:
  This verifies the business requirement that the program gracefully catches and reports JSON parsing issues. It's crucial in real-life scenarios where JSON data might be corrupt or not well-formed.

Scenario 4: Test the behaviour of __init__ function when an incorrect file_path is passed.
Details:
  TestName: test_incorrect_file_path
  Description: This test is intended to verify the the behaviour of `__init__` method when the file does not exist.
Execution:
  Arrange: Pass a file_path that does not exist to the `__init__` method.
  Act: Invoke the `__init__` method with the invalid parameters.
  Assert: Make sure that the function raises a FileNotFoundError.
Validation:
  This verifies the business requirement and functionality of the program to correctly handle situations where the file path pointed to does not exist.


```
"""

# ********RoostGPT********
import json
from pathlib import Path
from libs.constants import DEFAULT_ENCODING
import os
import pytest
from create_ml_io import CreateMLReader

class Test_CreateMlReaderInit:
    def test_correct_init(self):
        # Arrange
        json_path = "test_files/correct_json.json"
        file_path = "test_files/correct_file.txt"
        expected_filename = "correct_file.txt"

        # Act
        reader = CreateMLReader(json_path, file_path)

        # Assert
        assert reader.filename == expected_filename
        assert type(reader.shapes) == list
        assert reader.verified == False

    def test_incorrect_json_path(self):
        # Arrange
        json_path = "test_files/non_existant_file.json"
        file_path = "test_files/correct_file.txt"

        # Act & Assert
        with pytest.raises(FileNotFoundError):
            reader = CreateMLReader(json_path, file_path)

    def test_invalid_json(self, capsys):
        # Arrange
        json_path = "test_files/invalid_json.json"
        file_path = "test_files/correct_file.txt"

        # Act
        reader = CreateMLReader(json_path, file_path)

        # Assert
        captured = capsys.readouterr()
        assert "JSON decoding failed" in captured.out

    def test_incorrect_file_path(self):
        # Arrange
        json_path = "test_files/correct_json.json"
        file_path = "test_files/non_existant_file.txt"

        # Act & Assert
        with pytest.raises(FileNotFoundError):
            reader = CreateMLReader(json_path, file_path)
