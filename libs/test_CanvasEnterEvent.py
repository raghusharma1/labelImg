# ********RoostGPT********
"""
Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythbctesting3 using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=enterEvent_a077ed01a7
ROOST_METHOD_SIG_HASH=enterEvent_313793cd96


Scenario 1: Successful Call of enterEvent Method 
Details:
  TestName: test_enterEvent_success
  Description: This test verifies that the enterEvent method successfully calls the override_cursor method with the expected _cursor value.
Execution:
  Arrange: An instance of Canvas class should be created with all required parameters. Additionally, the _cursor attribute should be set to a specific cursor style.
  Act: Invoke the enterEvent method on the Canvas object and pass an event as a parameter.
  Assert: Check that the override_cursor function has been called with the defined _cursor value. 
Validation:
  This test is important to ensure that the enterEvent method sets or changes the cursor style correctly upon a user's entering event. This behavior is critical for providing accurate visual feedback to the user, enabling them to interact with the Canvas in the expected manner.

Scenario 2: enterEvent Method with No Initial _cursor Value
Details:
  TestName: test_enterEvent_without_initial_cursor
  Description: This test aims to validate the behavior of the enterEvent method when _cursor attribute of the Canvas instance is not initially set.
Execution:
  Arrange: Create an instance of Canvas class. Do not assign values to the _cursor attribute of the Canvas instance.
  Act: Call the enterEvent method on the Canvas instance, passing an event as a parameter.
  Assert: Confirm that the override_cursor function has been called with a default cursor value.
Validation:
  This test is crucial to ensure that the enterEvent method works properly even when the _cursor attribute is not assigned a value initially. It verifies the function's robustness and its ability to handle such situations by applying a default cursor style, ensuring uninterrupted user interaction.

Scenario 3: enterEvent Method with Different Cursor Values
Details:
  TestName: test_enterEvent_with_different_cursor_values
  Description: This test evaluates whether the enterEvent method changes the cursor style correctly when the _cursor attribute of the Canvas instance is altered.
Execution:
  Arrange: Instantiate the Canvas class and set the _cursor attribute to various cursor styles.
  Act: Invoke the enterEvent method on the Canvas instance with each different _cursor value, passing an event as a parameter.
  Assert: Verify each time that the override_cursor function is called with the applicable _cursor value.
Validation:
  This test is essential to make sure that the enterEvent method promptly and correctly changes the cursor style when a user interacts with different elements on the Canvas. Such functionality enhances user experience by providing relevant visual feedback.

"""

# ********RoostGPT********
import pytest
from unittest.mock import MagicMock
from PyQt5.QtGui import *
from PyQt5.QtCore import *
from PyQt5.QtWidgets import *
from canvas import Canvas

try:
    from libs.shape import Shape
except ModuleNotFoundError:
    pass

class Test_CanvasEnterEvent:

    @pytest.mark.positive
    def test_enterEvent_success(self):
        # Arrange
        event = MagicMock(spec=QEvent)
        canvas = Canvas()
        canvas._cursor = Qt.CrossCursor
        canvas.override_cursor = MagicMock()

        # Act
        canvas.enterEvent(event)

        # Assert
        canvas.override_cursor.assert_called_once_with(Qt.CrossCursor)

    @pytest.mark.negative
    def test_enterEvent_without_initial_cursor(self):
        # Arrange
        event = MagicMock(spec=QEvent)
        canvas = Canvas()
        canvas.override_cursor = MagicMock()

        # Act
        canvas.enterEvent(event)

        # Assert
        canvas.override_cursor.assert_called_once_with(Qt.ArrowCursor)

    @pytest.mark.regression
    def test_enterEvent_with_different_cursor_values(self):
        # Arrange
        cursor_values = [Qt.CrossCursor, Qt.ClosedHandCursor, Qt.OpenHandCursor]
        event = MagicMock(spec=QEvent)
        canvas = Canvas()
        canvas.override_cursor = MagicMock()

        # Act & Assert
        for cursor in cursor_values:
            canvas._cursor = cursor
            canvas.enterEvent(event)
            canvas.override_cursor.assert_called_with(cursor)
            canvas.override_cursor.reset_mock()
